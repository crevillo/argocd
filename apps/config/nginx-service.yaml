apiVersion: v1
kind: Service
metadata:
  name: my-nginx
  namespace: argocd
  labels:
    run: nginx
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: http
  selector:
    app: nginx
    tier: backend
apiVersion: v1
kind: Service
metadata:
  name: my-nginx
  namespace: argocd
  labels:
    run: nginx
spec:
  selector:
    app: nginx
    tier: backend
  ports:
  - protocol: TCP
    port: 85
    targetPort: http
